<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <title>Login</title>
    <style>
        * {
            padding: 0;
            margin: 0;
            box-sizing: border-box;
            font-family: helvetica, arial, sans-serif;
        }
        body{
            background-color: #5A5858;
          /* background-image: url('hpage/images/banner.jpg');
          background-size: cover; */
        }

        header {
            height: 100vh;
            width: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            user-select: none;
        }

        .login {
            text-align: center;
            border-radius: 15px;
            padding: 30px;
            background-color:white;
            transition: all 1s;
            opacity: 0.9;
        }

        /* .login:hover{
            transform: scale(1.1);
            -webkit-box-shadow: 0px 1px 42px 8px rgba(52,152,219,1);
            -moz-box-shadow: 0px 1px 42px 8px rgba(52,152,219,1);
            box-shadow: 0px 1px 42px 8px rgba(52,152,219,1);
        } */

        .login h3 {
            color: #3498db;
            font-size: 3.5ex;
            margin-top: 2.5ex;
            margin-bottom: 2.5ex;
        }

        .login #fields {
            padding: 1rem;
            width: 90%;
            margin: 20px 0px;
            border: none;
            border-bottom-style: groove;
        }

        .login #fields:focus {
            outline: none;
        }

        .login #login {
            background-color: #3498db;
            border-radius: 10px;
            width: 20%;
            margin-top: 30px;
            padding: 10px;
            color: white;
            border: none;
            cursor: pointer;
        }

        .login #login:hover {
            background-color: #201D2D;
        }

        .login img {
            margin-bottom: 15px;
            width: 35%;
            height: 10ex;
            transition: all 1s;
        }

        .login img:hover {
            transform: scale(1.1);
        }

        .login #error {
            color: red;
        }
        @media screen and (max-width: 630px) {
          header{
            height: 100vh;
            width: 90%;
            user-select: none;
            margin-left: 33px;
          }
        }
        @media screen and (max-width: 520px) {
          header{
            height: 90vh;
            width: 90%;
            user-select: none;
            margin-left: 26px;
          }
          .login h3 {
              font-size: 3ex;
          }
        }
        @media screen and (max-width: 450px) {
          header{
            height: 90vh;
            width: 90%;
            user-select: none;
            margin-left: 20px;
          }
          .login h3 {
              font-size: 2.5ex;
          }
        }
    </style>
</head>
<body>
    <header>
        <div class="login">
            <h3> Welcome to E-Classroom </h3>
            <form method="POST" action="/auth/login">
                <p><i class="fa fa-user-o" aria-hidden="true"><input type="text" name="username" id="fields" placeholder="Enter Username" required/></p></i>
                    <p><i class="fa fa-key" aria-hidden="true"><input type="password" name="password" id="fields" placeholder="Enter Password" required/></p></i>
                        <p id="error"><%= msg%></p>
                        <p><input type="submit" class="btn btn-primary" value="Login" id="login" /></p>
            </form>
        </div>
    </header>
</body>
</html>
